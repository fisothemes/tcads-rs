# TwinCAT ADS Server

A framework for building custom ADS servers and devices in Rust.

> This crate is a placeholder. It is published so that [`tcads`][tcads] can re-export it as
> `tcads::server`, but it contains no code yet. Nothing here is usable.

[tcads]: https://github.com/fisothemes/tcads-rs/tree/master/packages/tcads
